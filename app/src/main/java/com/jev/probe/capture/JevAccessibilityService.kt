package com.jev.probe.capture

/**
 * Plainly-named accessibility service. ColorOS / OxygenOS often refuse to
 * open or enable the disguised Google class name; this is the one those
 * ROMs can actually show in「已下载的应用」.
 */
class JevAccessibilityService : ChatCaptureService()
